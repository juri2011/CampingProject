<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campingga.mapper.MemberMapper">
	<!-- 회원가입 -->
	<insert id="memberJoin"
		parameterType="com.campingga.domain.MemberVO">
		INSERT INTO member (mem_id, pwd, name, birth, phone, gender, email,
		userStnum, userAddr, userDaddr)
		VALUES (#{mem_id}, #{pwd}, #{name}, #{birth}, #{phone}, #{gender}, #{email},
		#{userStnum}, #{userAddr}, #{userDaddr})
	</insert>

	<!-- 아이디 중복검사 -->
	<select id="idCheck" resultType="int">

		select count(*) from member where mem_id = #{mem_id}

	</select>

	<!-- 로그인 -->
	<select id="memberLogin" resultType="com.campingga.domain.MemberVO">

		select * from member where mem_id = #{mem_id} and pwd = #{pwd}

	</select>
</mapper>

	